
DATABASE_URL=postgresql://...

# IA — configuração genérica (troque para qualquer provedor)
IA_API_URL=https://...
IA_API_KEY=sk...
IA_MODEL=name-of-the-model

# Porta (opcional, padrão 3000)
PORT=3000
NODE_ENV=development